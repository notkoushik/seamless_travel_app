<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================ -->
    <!-- PERMISSIONS -->
    <!-- ============================================ -->

    <!-- Internet for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera for passport scanning and face recognition -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- NFC for passport reading -->
    <uses-permission android:name="android.permission.NFC" />

    <!-- File access for document storage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- ============================================ -->
    <!-- FEATURES -->
    <!-- ============================================ -->

    <!-- Camera is required for the app -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <!-- NFC is optional but supported -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <!-- ============================================ -->
    <!-- APPLICATION -->
    <!-- ============================================ -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SeamlessTravelApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- ============================================ -->
        <!-- MAIN ACTIVITY -->
        <!-- ============================================ -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SeamlessTravelApp">

            <!-- Launch intent -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- NFC intent filter for passport reading -->
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
            </intent-filter>

            <!-- NFC Tech filter configuration -->
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />

        </activity>

        <!-- ============================================ -->
        <!-- SmartScanner Activity (Auto-registered from AAR) -->
        <!-- ============================================ -->

        <!-- SmartScanner Activity is automatically included from the library AAR
             No need to declare it manually here -->

        <!-- ============================================ -->
        <!-- FILE PROVIDER (for image sharing) -->
        <!-- ============================================ -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ============================================ -->
        <!-- ML KIT METADATA (for Face Recognition) -->
        <!-- ============================================ -->

        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="face" />

    </application>

</manifest>